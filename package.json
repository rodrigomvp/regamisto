{
  "name": "regamisto",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Simulador de footprint de rega por canhão — camada model/ (motor físico).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
